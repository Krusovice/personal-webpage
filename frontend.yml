version: "3.9"

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: my-frontend:latest
    container_name: react-frontend
    ports:
      - "80:80"
    restart: unless-stopped
